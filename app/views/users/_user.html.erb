#coding: utf-8
<li>
  <%= gravatar_tag user, :size => 30 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
  ----  <%= link_to "удалить", user,:method => :delete, :confirm => "Вы уверены?",
                                    :title => "удалить #{user.name}" %>
  <% end %>
</li>